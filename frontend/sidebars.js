// @ts-check

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  // Minimal sidebar configuration for clean homepage
  tutorialSidebar: [
    'intro', // Only include the intro page for a minimal sidebar
  ],
};

module.exports = sidebars;